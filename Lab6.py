
"""
                             Plain English

To start, we will generate a random interger between 1 and 28, and based
on the result of the random number, we check to see if it falls under a certain range. 
If the number is greater than 15, then the results will be "Cherries",
otherwise if the number > 10, then the result will be "Orange", 
otherwise if the number > 5, then the result will be "Plum", 
otherwise if the number > 2, then the result will be "Melon",
otherwise if the number > 1, then the resukr will be "Bell",
if the number is not any of the above, then the result will be "Bar'

We iterate over using a loop three times and print the results to the 
user. As an example "Plum Cherries"

"""
"""
                            Pseudocode

import random
num = generate random number

If the number is > 15, 
    then the results will be "Cherries"
otherwise if the number > 10, 
    then the result will be "Orange" 
otherwise if the number > 5, 
    then the result will be "Plum"
otherwise if the number > 2, 
    then the result will be "Melon"
otherwise if the number > 1, 
    then the resukr will be "Bell"
otherwise, 
    then the result will be "Bar"

loop three times
    print the output (fruit) to the user

"""
